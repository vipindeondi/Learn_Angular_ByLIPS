import { EmailvalidationDirective } from './emailvalidation.directive';

describe('EmailvalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailvalidationDirective();
    expect(directive).toBeTruthy();
  });
});
